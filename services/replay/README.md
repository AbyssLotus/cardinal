# services/replay/

Vol. V Ch. 4's alarm system and Ch. 8's time travel, as a service.

Planned: twin-run orchestration with per-tick hash comparison, save-splice runs,
golden-transcript recording and verification, divergence bisection (auto-localize the
first differing proposal, Ch. 8 §8.2), restore-to-tick + replay-forward time travel,
and counterfactual branching (labeled studies, never mergeable — Vol. II's rollback
law).

**Law:** a determinism failure is a release blocker, never a known issue
(Vol. V Ch. 4 §4.4.10); a flaking twin run IS the bug report — no quarantine (§4.5).
