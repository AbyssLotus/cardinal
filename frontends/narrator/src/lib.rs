//! # Narrator frontend -- Vol. V Ch. 9
//!
//! Describes committed reality; it never computes it. The narrator consumes entitled
//! observation streams and turns committed state into prose -- and a full tick runs with it
//! DISABLED (Vol. V Ch. 9 §9.5.2). LLM output is never a dependency of state computation.
