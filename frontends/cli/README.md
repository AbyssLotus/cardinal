# frontends/cli/

The deterministic grammar: new / play / tick / inspect / validate, grown against the
new engine (the POC's cli/ is the archived ancestor). Headless operation is complete
and permanent (Vol. V Ch. 9 §9.5.2) — every soak and every CI run drives the world
from here with no model present.
