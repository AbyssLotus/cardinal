# tests/kernel/

Pyramid layers 1 and 3 (Vol. V Ch. 8 §8.3). Unit: store-contract conformance
(Vol. V Ch. 2 §2.1 — any store implementation certifies here), tick-stage behavior,
hermetic system evaluation, hash canonicality. Determinism: twin runs diffing per-tick
hashes, save-splice, golden transcripts (the POC's byte-identical combat is the
validated pattern), chaos ordering once parallelism exists (Vol. V Ch. 4 §4.2).
Execution-profile equivalence proofs live here too (Vol. V Ch. 3 §3.5.10).
